<tr id="<%= dom_id(audit.site, :audits_table) %>" class="ac-row-link">
  <%= audit_cell(audit, 'site') %>

  <% @displayed_columns.each do |column| %>
    <%= audit_cell(audit, column) %>
  <% end %>

  <%= audit_cell(audit, 'last_audit_at') %>
</tr>
